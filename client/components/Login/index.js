import React, { useReducer, useContext } from 'react';
import './login.scss';

const Login = () => {
    return (
        <div className="login-wrapper">
            <h1>ShitChat</h1>
        </div>
    )
};

export default Login;